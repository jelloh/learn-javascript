import { Component } from '@angular/core';


@Component({
  //selector: '', <- we don't use a selector for this one
  // Because it will only show up when the user is on the about page
  // it won't be on every page...



  templateUrl: './about.html',
  styleUrls: ['./about.css']
})

export class aboutComponent {



}
